$("#company-section").html("<%= j (render partial: 'flag_and_search_bar', locals: { country: @country, countries: @countries, sectors: @sectors}) %>")

<% unless params["first_page"] == "true" %>
  $(".choose-categories").click();
<% end %>
